import { PagingButtonDirective } from './paging-button.directive';

describe('PaginButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PagingButtonDirective();
    expect(directive).toBeTruthy();
  });
});
